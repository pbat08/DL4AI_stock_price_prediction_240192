# -*- coding: utf-8 -*-
"""api_server.py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/12QVRpB5trr3sq5OsjVOlOBGQeiiWDMTw
"""

!pip install tensorflow flask numpy

import os
import numpy as np
import tensorflow as tf
from flask import Flask, request, jsonify
import concurrent.futures

# Enable XLA for accelerated computation
tf.config.optimizer.set_jit(True)

app = Flask(__name__)

# ---------- 1. Parallel model loading + create fast predict ----------
tickers = [
    "VIC", "HPG", "VCB", "GAS", "PLX",
    "BID", "FPT", "MBB", "MSN", "VNM"
]

models = {}   # will store: (fast_predict_fn, timesteps, num_features)

def load_one_ticker(ticker):
    # Adapt this path to where your .h5 files live
    path = f"models/{ticker}_price_model.h5"
    model = tf.keras.models.load_model(path, compile=False)

    input_shape = model.input_shape          # (None, timesteps, features)
    timesteps = input_shape[1]
    num_feat = input_shape[2]

    # Create a tf.function with a concrete input signature
    input_spec = tf.TensorSpec(shape=(1, timesteps, num_feat), dtype=tf.float32)
    @tf.function(input_signature=[input_spec])
    def fast_predict(x):
        return model(x, training=False)

    # Pre‑warm the function (avoid first‑request slowness)
    dummy = np.zeros((1, timesteps, num_feat), dtype=np.float32)
    fast_predict(dummy)

    return ticker, fast_predict, timesteps, num_feat

# Use a thread pool to load models in parallel (overlap I/O)
with concurrent.futures.ThreadPoolExecutor(max_workers=4) as executor:
    futures = [executor.submit(load_one_ticker, t) for t in tickers]
    for future in concurrent.futures.as_completed(futures):
        ticker, fast_fn, tsteps, nf = future.result()
        models[ticker] = (fast_fn, tsteps, nf)
        print(f"✅ {ticker} loaded and pre‑warmed")

# ---------- 2. Predict endpoint ----------
@app.route('/predict', methods=['POST'])
def predict():
    data = request.get_json()
    ticker = data.get("ticker")
    features = data.get("features")   # dict with Open, High, Low, Close, Volume

    if ticker not in models:
        return jsonify({"error": f"Unknown ticker. Choose from {list(models.keys())}"}), 400

    fast_predict, timesteps, num_feat = models[ticker]

    # Build input array from numeric values
    feature_list = [features["Open"], features["High"], features["Low"],
                    features["Close"], features["Volume"]]
    input_array = np.array([feature_list], dtype=np.float32)

    # Reshape to (1, timesteps, num_features) by tiling the single row
    input_array = np.tile(input_array.reshape(1, 1, num_feat), (1, timesteps, 1))

    # Fast prediction (graph execution)
    pred = fast_predict(input_array).numpy().tolist()
    return jsonify({"ticker": ticker, "prediction": pred})

# ---------- 3. Main ----------
if __name__ == '__main__':
    port = int(os.environ.get("PORT", 8000))
    app.run(host='0.0.0.0', port=port, debug=False)